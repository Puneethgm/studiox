# Internal address of the Go API. Used by:
#   - Next.js rewrites in next.config.mjs (so /api/* in the browser proxies here)
#   - Server-side RSC fetches (lib/auth.ts, lib/public.ts)
#
# The browser NEVER sees this URL — all client-side calls are relative paths.
API_BASE_URL=http://localhost:8080
